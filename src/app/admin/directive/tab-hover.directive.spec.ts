import { TabHoverDirective } from './tab-hover.directive';

describe('TabHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TabHoverDirective();
    expect(directive).toBeTruthy();
  });
});
